import { Node } from './node';
export class FindInNodes {
  constructor(private nodes: Node[]) {}

  public findByNode(node: Node): Node {
    return null;
  }

  public findById(id: number): Node {
    return null;
  }
}
