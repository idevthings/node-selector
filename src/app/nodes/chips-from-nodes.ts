import { Chip } from '../chips/chip';
import { Node } from './node';

export class ChipsFromNodes {

  private chips: Chip[] = [];

  constructor(private nodes: Node[]) {
    const _nodes = this.nodes.
  }
}
